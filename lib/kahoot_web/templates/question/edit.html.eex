<h1>Edit Question</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.question_path(@conn, :update, @quizz_id, @question)) %>

<span><%= link "Back", to: Routes.question_path(@conn, :index, @quizz_id) %></span>
